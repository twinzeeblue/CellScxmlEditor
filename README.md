# Cell SCXML Editor

這是一個為 CellApps 專案量身打造的 VS Code 圖形化狀態機編輯器。

## 📖 文件索引

詳細的產品說明與技術文件請參考 `docs/` 目錄：

- **[產品規格書 (Product Specification)](docs/PRODUCT.md)**: 了解產品功能、核心價值與操作方式。
- **[技術規格書 (Technical Specification)](docs/TECHNICAL.md)**: 深入技術架構、資料流程與開發環境設置。
- **[開發路線圖 (Roadmap)](docs/ROADMAP.md)**: 查看目前進度與未來開發計畫。


## 📐 座標系統與視圖說明

本編輯器提供兩種座標資訊，協助您進行精確佈局與除錯：

1.  **圖像座標 (Diagram Cursor)**
    *   **定義**：即「絕對座標」，以圖表內容的左上角 (0,0) 為基準。
    *   **用途**：State (Node) 與 Transition (Edge) 的位置數據皆使用此座標系儲存。
    *   **特性**：座標值固定，不會隨視窗縮放或平移而改變。這是實際寫入檔案的數值。

2.  **畫布座標 (Canvas Cursor)**
    *   **定義**：即「相對座標」，以編輯器可視區域的左上角 (0,0) 為基準。
    *   **用途**：用於判斷滑鼠在目前螢幕畫面上的位置。
    *   **特性**：會隨視窗捲動 (Pan) 或縮放 (Zoom) 而即時改變。

> **換算關係**：`畫布座標 = (圖像座標 * Zoom Level) + [Pan X, Pan Y]`

## 🖥️ UI 操作元件名稱參照 (中英對照)

為了維持操作介面的一致性，以下將介面元件分為「畫布 UI」與「側邊屬性介面」兩類進行說明：

### 1. 畫布介面 (Canvas UI)
直接顯示在畫布區域上方或下方的操作元件。

| 中文名稱     | 對應英文/功能                       | 顯示位置                    |
| :----------- | :---------------------------------- | :-------------------------- |
| **自動佈局** | Auto Layout (Dagre)                 | 畫布左上角按鈕              |
| **縮放控制** | Zoom Controls (Zoom In/Out/Fit)     | 畫布左下角按鈕              |
| **座標顯示** | Coordinate Display (Diagram/Canvas) | 畫布左下角面板              |
| **除錯資訊** | Debug Info (Coordinates & Bounds)   | 畫布右上角面板              |
| **導航提示** | Navigation Hints                    | 畫布下方面板                |
| **預覽縮圖** | MiniMap                             | 畫布右下角 (點擊可快速跳轉) |

### 2. 側邊介面 (Side Interface)
選取元素後於右側顯示的屬性編輯面板。

| 中文名稱                  | 對應英文/功能         | 顯示位置                    |
| :------------------------ | :-------------------- | :-------------------------- |
| **State Properties**      | 狀態屬性面板標題      | 側邊欄頂部                  |
| **Transition Properties** | 轉換屬性面板標題      | 側邊欄頂部                  |
| **循環 (Loops)**          | Self-Loop Transitions | State 屬性面板              |
| **離開**                  | Outgoing Transitions  | State 屬性面板              |
| **進入**                  | Incoming Transitions  | State 屬性面板              |
| **目前無自循環**          | No Loops state        | State 屬性面板 (Loops 區塊) |
| **無離開的 transitions**  | No outgoing edges     | State 屬性面板 (離開區塊)   |
| **無進入的 transitions**  | No incoming edges     | State 屬性面板 (進入區塊)   |
| **開始 State**            | Source State          | Transition 屬性面板         |
| **結束 State**            | Target State          | Transition 屬性面板         |

## 🚀 快速開始

1.  **安裝依賴**：
    ```bash
    cd scxml-extension
    npm install
    ```
2.  **編譯**：
    ```bash
    npm run compile
    ```
3.  **運行**：
    在 VS Code 中按下 `F5` 啟動 Extension Development Host。

---
*Developed with Antigravity AI.*
